# TODO

- [ ] Update Navbar so the nav entry shows **Login** when logged out and **Logout** when authenticated.
- [ ] Implement Logout button that clears JWT token and navigates to `/login`.
- [ ] Verify behavior manually: login -> button switches to logout without refresh; logout -> login link appears.

